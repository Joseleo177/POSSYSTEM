const fetch = require('node-fetch');

async function check() {
  try { // simulating the query logic myself
    const { Sequelize } = require('./src/models');
    // ... just run the query from db directly
  } catch(e) {}
}
